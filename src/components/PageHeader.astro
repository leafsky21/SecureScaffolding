---
const { eyebrow, title, intro } = Astro.props;
---
<section class="section"><div class="wrap section-head"><div><div class="eyebrow">{eyebrow}</div><h1>{title}</h1></div>{intro && <p class="lead">{intro}</p>}</div></section>
